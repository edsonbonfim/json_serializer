name: json_serializer
description: A versatile Dart package for effortless JSON serialization and deserialization without the need for code generation or reflection.
version: 0.2.0
repository: https://github.com/edsonbonfim/json_serializer

environment:
  sdk: ^3.1.1

dev_dependencies:
  lints: ^2.0.0
  test: ^1.21.0
